# Andorra Viva

Aplicación web (Vite + React + TypeScript) para descubrir experiencias en el Principado de Andorra.

## Desarrollo

- `npm install` — instala dependencias (incluye la documentación en `documentation/` vía `postinstall`).
- `npm run dev` — servidor de desarrollo (puerto 8080).
- `npm run build` — build de producción.
- `npm run test` — tests con Vitest.
